
// Simple component to display the current player's turn
const CurrentPlayerStatus = ( {currentPlayer} ) => {

  return (
      <p>{currentPlayer}'s Turn</p>
  );
};

export default CurrentPlayerStatus;
